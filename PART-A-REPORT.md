# MizanFlow — تقرير إغلاق PART A

## المنجز
- **إثبات البريد:** جدول رموز hashed، انتهاء 24 ساعة، استخدام مرة واحدة، إعادة إرسال دون كشف وجود الحساب، ومنع تسجيل الدخول قبل التأكيد.
- **Forgot/Reset Password:** استجابة مقاومة لتعداد الحسابات، رمز hashed صالح 30 دقيقة، كلمة مرور bcrypt cost 12، وإبطال كل الجلسات بعد إعادة التعيين.
- **MFA:** TOTP RFC 6238 (SHA-1، 6 أرقام، 30 ثانية)، قبول نافذة ±30 ثانية، إعداد/تفعيل/تعطيل، و8 رموز استرداد hashed أحادية الاستخدام.
- **الأعضاء والدعوات:** عرض الأعضاء والدعوات، إنشاء/إلغاء دعوة، قبول الدعوة بالبريد نفسه، تغيير الدور والحالة، حذف عضو، حماية OWNER، وتسجيل عمليات الإدارة في AuditLog.
- **الواجهات:** صفحات تأكيد البريد، نسيان/إعادة تعيين كلمة المرور، قبول الدعوة، وحقل MFA في تسجيل الدخول.

## الاختبارات المنفذة
- 6/6 اختبارات ناجحة: Base32، TOTP، recovery codes، RBAC، وحضور نماذج schema المطلوبة.
- تعذر تشغيل Prisma migrations وNext.js build في حاوية التنفيذ الحالية لأن Docker غير مثبت والوصول إلى npm registry غير متاح؛ الأرشيف الوارد لا يحتوي `package-lock.json` أو `node_modules`، ومحاولة `npm install --offline` فشلت لعدم وجود `@prisma/client` في cache.
- لذلك لا أدّعي نجاح build أو التكامل مع PostgreSQL في هذه الحاوية. أوامر التحقق المطلوبة على جهاز متصل موضحة أدناه.

## تحقق محلي نهائي
```bash
cp .env.example .env
docker compose up -d
npm install
npm run db:generate
npm run db:migrate -- --name part_a_identity_team
npm run db:seed
npm test
npm run typecheck
npm run lint
npm run build
```

## ملاحظة إنتاجية
`src/lib/mailer.ts` هو adapter آمن للتطوير فقط. يجب ربطه بمزوّد بريد فعلي قبل النشر، وعدم إرجاع روابط التطوير في `NODE_ENV=production` (الكود يطبق ذلك بالفعل).

## قرار الانتقال
لم يبدأ Step 3. يوصى بالانتقال إليه فقط بعد نجاح أوامر التحقق المحلي النهائي أعلاه في بيئة تتوفر فيها Docker والاعتماديات.
