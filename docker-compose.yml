services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mizanflow
      POSTGRES_PASSWORD: mizanflow
      POSTGRES_DB: mizanflow
    ports: ["5432:5432"]
    volumes: ["mizanflow_pg:/var/lib/postgresql/data"]
volumes:
  mizanflow_pg:
